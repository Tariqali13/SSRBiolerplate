import React from "react";
import { HeroSection } from "../../../components";

const HeroExample = () => {
  return (
    <section id="hero-block" className="hero-examples">
      <HeroSection
        heading="Free Business Lessons"
        para="One platform to replace them all. All your business in one place."
      />
    </section>
  );
};

export { HeroExample };
